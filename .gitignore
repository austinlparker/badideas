# Built in CI by mysql-wasm/build.sh. MySQL itself still does not belong in
# git history, only in your browser's memory.
site/mysql-wasm/
.cache/

# Vendored in CI by tools/vendor-php-wasm.sh
site/php-wasm/

node_modules/
package-lock.json
